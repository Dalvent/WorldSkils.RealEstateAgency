<UserControl x:Class="RealEstateAgency.Controls.AddEditTextBoxControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RealEstateAgency.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="35" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="25*"/>
            <ColumnDefinition Width="75*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" FontSize="18" Text="{Binding Title}"/>
        <TextBox Name="inputTextBox" Grid.Column="1" Text="{Binding InputText}" MaxLength="{Binding MaxLength}"/>
    </Grid>
</UserControl>
